# Mock LLM 서버 Service (KIND 환경, DLD-610)
apiVersion: v1
kind: Service
metadata:
  name: mock-llm
  namespace: featuremap
  labels:
    app: mock-llm
spec:
  type: NodePort
  ports:
    - port: 3102
      targetPort: 3102
      nodePort: 30102
      protocol: TCP
  selector:
    app: mock-llm
