# Mock GitHub 서버 Deployment (KIND 환경, DLD-610)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-github
  namespace: featuremap
  labels:
    app: mock-github
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mock-github
  template:
    metadata:
      labels:
        app: mock-github
    spec:
      containers:
        - name: mock-github
          image: mock-github:e2e
          imagePullPolicy: Never
          ports:
            - containerPort: 3101
              protocol: TCP
          env:
            - name: PORT
              value: "3101"
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3101
            initialDelaySeconds: 5
            periodSeconds: 5
